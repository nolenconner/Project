package pitchMatchup;

public class pitchMatchup {
	
	public static double fourSeamMatchup(double hitterwoba, double hitterSlug, double pitcherPercentage) {
		return (hitterwoba + hitterSlug) * (pitcherPercentage / 100);
		}
	
	public static double sliderMatchup(double hitterwoba, double hitterSlug, double pitcherPercentage) {
		return (hitterwoba + hitterSlug) * (pitcherPercentage / 100);
		}
	
	public static double sinkerMatchup(double hitterwoba, double hitterSlug, double pitcherPercentage) {
		return (hitterwoba + hitterSlug) * (pitcherPercentage / 100);
		}
	
	public static double curveballMatchup(double hitterwoba, double hitterSlug, double pitcherPercentage) {
		return (hitterwoba + hitterSlug) * (pitcherPercentage / 100);
		}
	
	public static double changeupMatchup(double hitterwoba, double hitterSlug, double pitcherPercentage) {
		return (hitterwoba + hitterSlug) * (pitcherPercentage / 100);
		}
	
	public static double cutterMatchup(double hitterwoba, double hitterSlug, double pitcherPercentage) {
		return (hitterwoba + hitterSlug) * (pitcherPercentage / 100);
		}
	
	public static double splitterMatchup(double hitterwoba, double hitterSlug, double pitcherPercentage) {
		return (hitterwoba + hitterSlug) * (pitcherPercentage / 100);
		}

}
